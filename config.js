'use strict'

const config = {
  SCREEN_WIDTH: 800,
  SCREEN_HEIGHT: 600,
  SPRITE_WIDTH: 32,
  SPRITE_HEIGHT: 64,
  VELOCITY: 2,
  YES_BOUND: this.SCREEN_WIDTH / 4,
  NO_BOUND: (this.SCREEN_WIDTH / 4) * 3
}

module.exports = config
